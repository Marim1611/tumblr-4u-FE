server { 
 listen 80;
 server_name front_img;
 location / {
   # This would be the directory where your vue app's static files are stored at
   root /usr/share/nginx/html;
   try_files $uri /index.html;
 }
}